# --- 核心深度学习框架 ---
# PyTorch, 我们的基础
torch
torchvision

# --- 图像处理与科学计算 ---
# OpenCV, 用于所有图像的读写、变换操作
opencv-python
# Numpy, 用于高效的数组计算
numpy
# Pandas, 用于读取和解析我们的CSV标签文件
pandas
# Scipy, 用于计算评估指标中的欧氏距离
scipy

# --- 日志、进度条与其他工具 ---
# TensorboardX, 用于被 TensorboardLoggerHook 调用，记录训练日志
tensorboardX
# TQDM, 用于在终端显示美观的训练进度条
tqdm